kind = "web"
previewPath = "/"
title = "Yalla Viral"
version = "1.0.0"
id = "artifacts/yalla-viral"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 23010

[services.development]
run = "pnpm --filter @workspace/yalla-viral run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/yalla-viral", "run", "build" ]
publicDir = "artifacts/yalla-viral/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "23010"
BASE_PATH = "/"
